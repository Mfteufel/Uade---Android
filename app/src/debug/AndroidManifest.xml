<?xml version="1.0" encoding="utf-8"?>
<!--
    Overlay solo de debug: agrega el network security config que habilita HTTP
    en texto plano hacia el backend local (ver res/xml/network_security_config.xml
    en este mismo source set). No afecta el manifest de release.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:networkSecurityConfig="@xml/network_security_config" />

</manifest>
