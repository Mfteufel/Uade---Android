<?xml version="1.0" encoding="utf-8"?>
<!--
    Solo para builds debug (ver app/src/debug/AndroidManifest.xml): permite HTTP
    en texto plano hacia el alias de host del emulador (10.0.2.2) y localhost,
    para poder apuntar NetworkModule a un backend corriendo en la máquina de
    desarrollo (API_BASE_URL en local.properties) sin tocar la URL de
    producción (Railway, HTTPS, no necesita esta excepción).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
