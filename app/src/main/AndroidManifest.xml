<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Necesario para publicar el artículo y pausar/reactivar contra la API_Rest (Punto 5). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permiso para chequear la red (Modo offline, Punto 6). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Desbloqueo por huella/cara/PIN del dispositivo (Punto 1). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!--
        Elegir fotos de la galería al publicar (Punto 5). Dos permisos porque el
        de medios reemplazó al de almacenamiento a partir de API 33: se declaran
        los dos y se pide en runtime el que corresponda según versión (ver
        PublicarFotosFragment), nunca alcanza con declararlo acá solo.
    -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".login.RondaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TPO">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>